# CFPB_complaint_narrative_clusters
This repository implements a two-step clustering pipeline for analyzing consumer complaint narratives. It combines SBERT embeddings, UMAP dimensionality reduction, and HDBSCAN clustering to uncover latent issue patterns and support validation using structured complaint data (e.g., product, issue, and date).
